name: Delete ghost workflow runs

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/delete-ghost-runs.yml
  workflow_dispatch:

permissions:
  actions: write
  contents: read

jobs:
  delete-ghost-runs:
    runs-on: ubuntu-latest
    steps:
      - name: Delete stale ghost workflow runs
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          failed=0
          for run_id in 32985738882 32985487405 32985427909; do
            echo "Deleting workflow run ${run_id}"
            http_code="$(curl -sS -o "/tmp/delete-${run_id}.txt" -w '%{http_code}' \
              -X DELETE \
              -H "Accept: application/vnd.github+json" \
              -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/${REPO}/actions/runs/${run_id}")"
            cat "/tmp/delete-${run_id}.txt" || true
            echo
            echo "HTTP ${http_code}"
            if [[ "${http_code}" != "204" && "${http_code}" != "404" ]]; then
              failed=1
            fi
          done
          exit "${failed}"

# Trigger one-time cleanup run.
