name: Apply mobile Supertonic safety (legacy manual)

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
      - name: Legacy notice
        run: |
          echo 'The one-time mobile Supertonic safety migration has already been applied.'
          echo 'Use the mobile hosted playback workflow for current mobile audio fixes.'